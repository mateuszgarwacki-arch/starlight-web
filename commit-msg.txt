feat: recent jobs strip for quick navigation back to jobs  
  
- useJobHistory hook with sessionStorage tracking  
- RecentJobsStrip shows last 5 visited jobs on non-job pages  
- Chips show job number, name, and last scope visited  
- recordJobVisit wired into job detail, scope, and WO pages  
