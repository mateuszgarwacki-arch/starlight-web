feat(wo-docs): enlarge drawing/reference thumbnails for visual identification during reorder

Previous: 64x64 square with object-cover (centre crop) - landscape drawings cropped to
a tiny middle slice, indistinguishable without click-through to preview.

Now: 176x128 (A-series landscape ratio) with object-contain on white bg - whole
drawing visible, identifiable at a glance during drag-drop reorder. Order badge
and delete button scaled proportionally so they don't look lost on the larger
thumbnail.

Affects both drawing and reference doc types (shared code path).

Note: PDFs still fall back to a generic FileText icon (now larger but still
generic) - pdf.js rendering remains on the backlog.
