feat: fold Timesheets into Review nav + self-healing timesheet gaps (S52 cont.)

Nav:
- Remove standalone /review/timesheets sidebar item; it lives as the
  Timesheet gaps tab inside Review. The route still exists.
- Review badge now aggregates pending inbox items + open timesheet gaps,
  and lands on whichever queue has items (inbox first, else timesheet gaps).

Timesheet gap cron (migration timesheet_gap_self_heal):
- rpc_detect_timesheet_gaps: added clear_notifs CTE — dismisses the
  'timesheet_no_show' notification once the freelancer logs any hours for
  that date (the alert said "no hours logged", no longer true). The open
  flag already auto-resolves when they're no longer short.
- cron job 6 now re-runs a rolling 7-day window (was: yesterday only).
  This is what actually makes the self-heal fire — previously each date was
  processed once and never revisited, so backfills were never picked up.
  Idempotent: flags stay single via ON CONFLICT, notifications only fire
  for genuinely new no-shows.
