Handover Phase 3 complete — line overrides + WO notes (Turn C)

Authoring (edit page):
- Per-zone expandable list of quote lines. Each line has an include
  checkbox (default: included), a line-note textarea, and its WOs
  listed underneath with per-WO note textareas.
- Line inclusion writes to tbl_handover_line_overrides.is_included.
- Line note writes tbl_handover_line_overrides.notes; upserts the row
  with is_included=true if it didn't exist yet.
- WO note writes to tbl_handover_wo_notes.notes. Sparse row created
  only when non-empty, deleted when cleared to zero.
- Auto-save on blur for note fields; toggle saves instantly.
- Inline lines are loaded on-demand when the lines section is expanded
  to avoid hydrating every line for every zone up front.

Print (/reports/handover/[jobId]):
- Cover banner surfaces excluded_line_count — "N quote lines have
  been excluded from this handover" so the reader knows the doc
  was filtered, not accidentally short.
- Per-line note renders as a sky-blue left-bordered quote block
  under the line header, before the scope breakdown.
- Per-WO note renders as a smaller sky-blue strip between the WO
  description and the "Who knows" workers line.

Data layer was already shipped in the previous migration; this turn
is purely frontend consumption of existing JSON fields.

No schema change.
