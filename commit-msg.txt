S39+ Copy from scope to WO description

create-wo-dialog: new "Copy from scope" button next to Description label.
Prefers tbl_scope_items.description, falls back to item_name. Confirms
before replacing if WO description already has text. Hidden when scope
has neither field.

Description field bumped from <input> to <textarea rows={3}> so pasted
multi-line scope text actually shows.

ScopeContext gains description field (already in qry_scope_breakdown).

Files:
- src/components/create-wo-dialog.tsx (new props, copy handler, textarea)
- src/components/work-orders-panel.tsx (pass scope desc/name through)
- src/app/(dashboard)/jobs/[id]/scope/[scopeId]/page.tsx (same)
- src/lib/types.ts (ScopeContext.description)

No SQL. No new dependencies.
