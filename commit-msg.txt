fix(s28d): PM RPC — WO-attached BOM + Length-on-Metre cost conversion

- rpc_pm_job_overview v5:
    * bom_raw CTE now reaches quote_line via scope OR work_order
      (was scope-only, silently dropped WO-attached BOM rows)
    * New unit-to-base multiplier for timber/linear-trim rows where
      BOM row unit='Length' but material base unit is 'Metre':
      multiply by (standard_length / 1000). Fixes 3x2 Rounded Edge
      reading £6.60 instead of the correct £31.68 (4 × 4.8m × £1.65).
- TRACKER.md: Session 28d writeup; flags that admin scope page has
  the same Length-on-Metre bug and should be patched next session.
- No frontend changes, no deploy needed — RPC applied to prod DB
  via Supabase migration; changes are live on next page load.

Regression: total materials_total + job_items_total across Grosvenor's
89 lines ties to raw aggregation of BOM rows to the 1p, confirming no
double-counting from the dual-path join.
