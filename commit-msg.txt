Session 25e: Embed visible toast diagnostics

- LearningCapture: embed call now surfaces result/error via toast so we can see exactly what's happening (success count, HTTP status, Voyage error, or abort)
- AbortController with 5s timeout to prevent indefinite hang
- Purely diagnostic — will be simplified once root cause is confirmed
