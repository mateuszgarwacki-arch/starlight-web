S34: Cover page — restore A4 minimum height

Cover page was shrinking to content size because flowLayout dropped
min-height entirely. Keep min-height: 287mm (A4) so short cover content
still fills a full page, and only relax page-break-inside so longer
content can still spill to a 2nd printed page.

- app/traveller/page.tsx: flowLayout pageStyle now keeps minHeight:287mm
- app/globals.css: .traveller-cover drops min-height:auto override,
  keeps only page-break-inside:auto
