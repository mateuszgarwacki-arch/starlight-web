S40 wrap — TRACKER session entry + cleanup backlog

Documents the three small items shipped this session:
- S40a: Copy from scope to WO description (+ textarea bump)
- S40b: CAD breakdown surfaced to freelancer mobile (S30 convention update)
- S40c: Web Share API for mobile drawings/files (file blob, not URL)

Cleanup Backlog small/mechanical updated with two S40 follow-ups:
- Update 05_conventions.md §11 (project knowledge file, manual)
- Share-flow size threshold (conditional, only if 50MB+ PDFs feel slow)

Conventions promoted (in TRACKER session entry):
- Never share a signed URL when you can share the bytes
- Auto-fill defaults vs opt-in copy
- Capability-detect, then fall back

No code changes in this commit. Documentation only.
