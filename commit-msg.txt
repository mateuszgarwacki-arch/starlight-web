S36 mobile tasks: search, scope-as-label, no truncation, persist collapse, drop Live pill

- Sticky search bar at top of /m page, debounced 150ms
- Filters across description, scope name, job name, job number, activity label
- Auto-expands all jobs when search is active so matches are visible
- Scope headers redesigned as flat labels (Tag icon, muted, no bg, no uppercase)
  to stop freelancers mis-tapping them as buttons
- Removed truncation on activity label (was sliced at 12 chars) and description
  (was truncate class) - per no-truncation convention
- Collapsed job state now persists in localStorage per freelancer
- Dropped "Live" status pill on In-Progress WOs - worker pills already signal it
