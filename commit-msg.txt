S38d: Quote line badges — checklist pattern

Replaces ad-hoc booleans (interpretation_complete, kit_list_exported) with
a lookup + junction pattern. New check types = data, not migrations.

DB (migration s38d_quote_line_badges_checklist):
- tbl_check_types: lookup (check_code, label, display_order, badge_color)
- tbl_quote_line_checks: junction; presence of row = ticked
- Seeded with 'interpreted' and 'kit_list_ready'
- Backfilled 24 previously-ticked rows from interpretation_complete
- qry_quote_line_badges: unified feed — manual checks + derived materials_needed
- qry_dash_quote_stats: rewritten to read from junction
- Dropped tbl_quote_lines.interpretation_complete + kit_list_exported

Frontend (jobs/[id]/page.tsx):
- Loads all badges from qry_quote_line_badges in one query
- Generic toggleCheck(line, checkCode) replaces duplicated handlers
- Flags column now shows [I] [K] toggles + amber materials_needed chip
- "Done" column header renamed to "Flags"
- Removed orphaned Check icon import
