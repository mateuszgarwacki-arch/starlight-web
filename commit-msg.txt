fix(labels): target the loaded GT800, never the plastic-stock printer (S53)

The ...Plastic Barcode printer isn't loaded. getPrinter() now picks the
first non-"plastic" printer Browser Print can see, only falling back to
the default/first if no non-plastic printer exists — so labels always go
to the normal (paper) GT800 regardless of the Browser Print default.
