S39+ Expose CAD breakdown to freelancer mobile

The S30 convention filtered both cad_concept AND cad_breakdown out of
the freelancer-facing query. That overshot — cad_breakdown is by
definition the workshop's interpretation FOR the workshop, so hiding it
from the floor was wrong. Senior carpenters with SketchUp on a workshop
laptop should be able to download it during build.

Change:
- mobile-wo-docs.tsx filter now excludes only cad_concept (design source
  is still PM-only)
- New render branch for cad_breakdown — full-row download button with
  Wrench icon, mirrors the cut_list pattern
- DOC_ICONS gets cad_breakdown entry: amber Wrench, label "CAD Breakdown"

No size warnings, no Wi-Fi prompts, no permission tier — read-only
download for everyone on /m/wo/[woId]. Junior carpenters who download a
.skp they can't open just learn not to tap it again. Self-correcting.

Files:
- src/components/mobile-wo-docs.tsx (filter, DOC_ICONS, render branch)

No SQL. No deps. No API changes.
