fix: PDF extraction — add anthropic-beta header + robust JSON parsing

PDF documents sent to Claude API require the pdfs-2024-09-25 beta
header. Also hardened JSON parsing in both extract-cutlist and
extract-invoice routes to handle Claude responses with surrounding
text (extract first { to last } fallback).