S36b mobile tasks: Mine -> Recent filter rebuilt

- "Mine" was effectively dead — it showed only WOs with an open timer
  (same info as the mobile header widget), so freelancers lost sight of
  what they'd been working on as soon as they stopped their timer.
- Renamed filter to "Recent (N)"; hides when count is zero.
- New logic: any WO where freelancer has a non-archived time entry,
  including Complete WOs.
- Sorted by most-recent entry_id first so "where did I leave off"
  surfaces at the top.
- Added my_last_entry_id field on TaskCard; count in button.
- Renamed Complete status badge "Built" -> "Completed" on /m task row.
