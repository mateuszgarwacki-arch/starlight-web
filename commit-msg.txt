Session 25c: Learnings context + embed auth fixes

- qry_learnings_enriched: context_label now includes quote line + WO activity label + material/stock/supplier/freelancer when attached (previously showed only Job number for non-scope attachments)
- WO label resolved via join to tbl_master_lookups.lookup_value (activity_verb is FK, not text)
- LearningCapture: fire-and-forget /api/learnings/embed now includes Bearer token so it actually runs
