S45c — Correct quoted_workshop to match cost-breakdown.tsx

S45b shipped quoted_workshop with an invented exclude-list (everything
except Subcontracted/Provisional) — £112,615 for FA Leoube. That didn't
match the job page's Cost Analysis panel, which shows £8,850.

cost-breakdown.tsx already has the canonical definition: an INCLUDE-list
of bench-work categories — category text contains 'workshop', 'stock
pick' or 'stock-and-hire'. The close report should have reused it, not
invented a parallel one.

Fix: rewrote the quote_workshop CTE in rpc_job_close_report to the
include-list, mirroring cost-breakdown.tsx exactly. UI labels changed
"Workshop" -> "Workshop + stock" to match job-page terminology. No UI
logic change — components already read quoted_workshop.

Verified: FA Leoube returns quoted 135,405 / quoted_workshop 8,850 /
margin 81.8% — exact match to the job page Cost Analysis.

Debt: the workshop-quote rule now lives in two places (this RPC +
cost-breakdown.tsx). TRACKER backlog item added to consolidate into a
shared view if touched again.

Touched: src/components/job-complete-dialog.tsx,
         src/app/(dashboard)/reports/job-close/[jobId]/page.tsx,
         docs/03_database_schema.md, docs/TRACKER.md
RPC change applied via migration s45c_quoted_workshop_match_costbreakdown.
