fix(mobile): day-view "Add entry" without WO no longer crashes

Path B of handleBackfillSubmit (no WO chosen) was inserting into
tbl_tasks without `category`, which is NOT NULL with no default —
Postgres rejected with violates not-null constraint.

Now defaults to category='other'. The freelancer didn't categorise,
so 'other' is the honest signal: PM triages in /review/inbox (which
already handles re-routing to job_work / workshop_general / a WO).

Picked 'other' over 'workshop_general' on purpose: workshop_general
should mean a freelancer actively chose "this was workshop housekeeping",
not the default for "I didn't tell you what bucket this is".

Also: stale comment above the backfill LogSheet claimed the handler
rejects without a WO. It hasn't for a while — picker label already
reads "Work Order (optional)". Comment now matches reality.
