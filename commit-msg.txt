S37b audit follow-through — invariant watcher + dead code cleanup

Audit findings (post-S37):
- unit_to_base_multiplier column: zero consumers in code, unused in views.
  Dropped from qry_bom_enriched. Two dependent views (qry_wo_estimated_cost,
  qry_scope_estimated_cost) rebuilt via CASCADE.
- actual_unit_cost: zero write paths exist. Latent column. When invoice
  reconciliation eventually writes it, the invariant lives in the column
  comment waiting.
- All 7 active BOM write paths audited: every one maintains the invariant.
- tbl_invoice_lines: uses line_total directly for aggregation (not
  qty x unit_cost). Same bug class structurally absent.
- 51 invoice lines audited: 0 inconsistent (qty x unit_cost ~= line_total).

Removed:
- src/components/scope-bom.tsx: orphaned since S19 scope-page unification.
  No imports anywhere in codebase.

Added:
- qry_bom_invariant_violations: view that returns zero rows when the
  Length-on-Metre unit_cost invariant holds. Returns rows if a write path
  slips up. Wire to a Vercel cron later for proactive alerting.

Verified:
- Violations view: 0 rows
- bom_id 183 (screenshot row): GBP 31.68
- Scope 25 total: GBP 256.08
