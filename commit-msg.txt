feat(labels): cut once after the batch, not per label (S53)

buildZpl now sets ^MMT (tear-off, no cut) on every label except the last,
and ^MMC on the final label — so a run of N labels prints as one strip
and the cutter fires once after the last. Single label still cuts (it is
the last). Per Zebra/Loftware guidance for batch cutting.
