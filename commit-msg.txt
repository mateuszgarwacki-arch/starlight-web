Crew detail: show ad-hoc tasks in Activity tab with inline editing

- Activity tab now shows reviewed tasks (routed/approved/rejected) below WO entries
- Ad-hoc Tasks section with category badge, status badge, job reference, cost calc
- Inline editing: pencil icon opens hours + title editing (same pattern as WO entries)
- Tab count includes both WO entries and tasks
- Loads all task statuses: pending → amber panel, rest → timeline