Fix multi-page cutlist in job traveller — render every PDF page

- tbl_wo_documents cut_list PDFs: read numPages via pdf.js at data-load
  time so each PDF page becomes its own traveller page.
- pdfPageUrls[doc_id] now contains one URL per page (was always [url]
  regardless of page count, so anything past page 1 was silently dropped).
- ImagePage takes a pdfPage prop and renders that page (was hardcoded
  to pdf.getPage(1)).
- Multi-page cutlists labelled "Cut list (n/m)" so it's obvious in print.
- Falls back to length 1 if pdf.js isn't ready or getDocument fails,
  preserving the previous behaviour on error.

Affects: src/app/traveller/page.tsx
