docs(s50): TRACKER session log for S50d + S50e

Five deploys in S50 total, not three. The scroll-restore feature
that S50a shipped took three more attempts to actually land:

- S50a: feature ships, doesn't actually scroll on prod
- S50b: tooling policy (docs/06_tooling.md), no scroll fix
- S50c: first scroll fix (race-condition flag + scroll={false} on
  Link). Real bug, real fix, but not the one that mattered.
- S50d: SSR hydration trap. typeof window check in useState
  initialiser meant the active tab never actually switched, the
  WO rows were never in the DOM, querySelector returned null,
  no scroll. Fixed by moving the URL read into a post-mount
  useEffect.
- S50e: design reframe from Mateusz - "i want to be taken to
  quote line tab - that's from where i operate". Back-link now
  always lands on Quote Lines anchored to the line, regardless
  of whether the user came via a scope or a WO. Cleaner mental
  model, side benefit of removing the SSR trap from the critical
  path.

Schema summary updated: 5 deploys, 0 schema changes, lifetime
totals unchanged (57 tables, 34 views, 18 RPCs, 2 cron jobs).
