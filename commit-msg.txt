S51: move cut plan from TaskBrief to a dedicated page after PDFs

Mateusz: "cutlist view should be placed under parts list in print
traveller (normally from 3rd page)."

Page sequence per WO now reads:
  Task brief (BOM)  ->  source cutlist PDF(s)  ->  suggested cut plan
                    ->  drawings  ->  references

Changes:
- src/app/traveller/page.tsx
  - Drop CutPlanSection from TaskBrief signature + render
  - Add dedicated cut-plan page after the cut-list PDF loop, gated on
    any material having sheet_placements or length_bins. flowLayout so
    it can spill onto a second printed page when many materials.
  - hasCutPlanPage computed once per WO in both totalPages count loop
    and the page-rendering loop (separate scopes, separate declarations).
- docs/TRACKER.md
  - S51 entry: updated traveller paragraph and added a brief "Initial
    placement and refinement" note recording the move.

No schema. No DB writes. No new files.
