fix(S56b): make New Job modal scrollable (quote-import review)

The New Job modal box had no height cap and no internal scroll, and was
vertically centred in a fixed inset-0 overlay. A tall import-review (many
extracted quote lines) overflowed the viewport top and bottom, putting the
"Create job & save quote" button out of reach.

- modal box -> max-h-[90vh] flex flex-col overflow-hidden
- header, tab bar, manual-tab footer -> shrink-0 (pinned)
- import body + manual fields body -> overflow-y-auto min-h-0 (only body scrolls)

Applies to both tabs; short content still renders a short modal.
AddJobWithQuote unchanged. next build clean. docs(S56b) in TRACKER.
