Session 25f: Embed diagnostic endpoint + keepalive fetch

- GET /api/learnings/embed?diag=1 — unauth-required diagnostic that calls Voyage directly and reports what happens (key present? Voyage 200? dimensions?)
- LearningCapture: fire-and-forget fetch now uses keepalive:true so the request survives component unmount (this is the real fix for embeds never running)
- Error messages in POST route now include which step failed
