S33 tracker: backlog cleanup + proxy rename session entry

Documents-only commit for session 33 history:
- Removed closed items from cleanup backlog top section
  (middleware rename done, Phase 3 CAD library done from S32,
  S28d BOM bug verified fixed, dead file deleted)
- Updated Key Files table: middleware.ts -> proxy.ts
- Added full Session 33 entry with S28d verification evidence,
  smoke test plan for proxy rename, conventions added

No code changes. Everything in this commit has already shipped
via earlier commits e0de151 (cleanup) and a8210eb (proxy rename).
