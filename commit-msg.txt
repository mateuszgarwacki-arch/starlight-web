feat: allow creating multiple WOs from same job item

Job items with existing WO coverage can now be selected for additional
WO creation (e.g. BUILD then PAINT). Removed opacity dimming and
static checkmark — items are always selectable. Delete still blocked
for items with WOs.