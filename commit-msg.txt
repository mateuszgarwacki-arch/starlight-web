feat: job orders panel, invoices+orders side-by-side on job page

- New JobOrdersPanel component showing BOM items needing ordering + already ordered
- Outstanding items (amber) with material, scope, qty, cost
- Ordered items (green) with supplier, date, scope context
- Both panels in 2-column grid on lg+ screens
- Both auto-hide when no data, link to full Orders/Invoices pages
- h-full on both cards for matched heights