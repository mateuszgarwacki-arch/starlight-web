fix(review): make route-task pre-selection visible + clarify Approve Overhead

Route Task modal:
- Scroll the pre-routed WO into view on open (once). On busy jobs (e.g.
  13585 has 33 WOs) the pre-selected WO sat below the fold, so the modal
  looked like nothing was chosen and PMs re-hunted for the WO.
- Add a "Routing to: <scope> — <description>" banner in the footer so the
  current selection is always visible and a pre-routed task is one click
  (confirm + Route), not a search.

Review inbox:
- Tooltip on Approve Overhead clarifying it logs as general workshop
  overhead, not billed to a job (distinct from the in-modal Job Overhead
  bucket).
