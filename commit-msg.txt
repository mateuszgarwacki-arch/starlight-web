S39b Route-task modal redesign

- Extract routing modal to src/components/route-task-modal.tsx
- max-w-5xl (1024px) two-pane layout: jobs left, WOs right
- Job pane: search, sorted by event date, task's job pinned with badge, overhead-marker icon
- WO pane: overhead WO pinned to top with amber callout, regular WOs grouped by scope
- Auto-clears stale WO selection when job changes
- Closes on ESC
- Notification text distinguishes overhead routing vs regular WO
- Drop inline modal JSX + WOOption interface + stale imports from inbox page
