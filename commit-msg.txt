docs(s50b): tooling policy + MCP server reference

Codify which MCP tool to reach for in which situation so the choice
isn't relitigated each session. Born from S50a's two long-running
shell commands that hung for 8 and 17 minutes through DC before being
swapped to Vercel MCP / native build.

New file: docs/06_tooling.md
  - Decision matrix: file I/O → Filesystem; SQL → Supabase;
    deploys → Vercel MCP; short shell → windows-cli (cmd, workingDir).
  - Never run through shell-over-MCP: production builds, full-repo
    typechecks, fresh npm install, npm run dev. Anything >30s with
    streaming output is a poor fit for MCP request/response.
  - DC kept installed as fallback for stateful sessions (REPLs, SSH).

Updates:
  - docs/00_README.md: 06_tooling.md added to read-order (item 6) and
    update-triggers table; TRACKER now item 7.
  - docs/TRACKER.md: S50 covers S50a (code) + S50b (this doc commit);
    schema summary updated to 2 deploys, 0 schema changes.

No code changes.
