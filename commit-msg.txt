docs(tracker): S48 — WO docs UX polish + reusable PDF viewer

Session log entry covering:
- S48a: WO drawing thumbnails 64x64 -> 176x128 with object-contain and
  stripped-prefix caption underneath
- S48b: react-pdf-based PdfViewer + PdfThumb components (reusable across
  the system), wired into wo-documents-panel for both grid thumbnails
  and click-to-preview; routes all PDFs in the docs panel through the
  in-app viewer instead of new tabs / iframes
- S48c: Crew page PIN visibility investigation (declined - SP-002 ban
  on plaintext PIN storage), pattern recorded for future reference

Cleanup backlog:
- Handover PDF drawing rendering item updated to reflect that react-pdf
  + worker are now in place (just needs the multi-page print adaptation)
- Added: PDF thumbnail aspect-aware sizing (portrait PDFs top-cropped)
- Added: react-pdf transitive vulnerabilities recheck on each upgrade

No schema changes. Totals unchanged: 57 tables / 34 views / 18 RPCs / 2 cron jobs.
