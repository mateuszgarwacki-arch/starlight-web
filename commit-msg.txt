fix(labels): revert PDF fallback to seated 50x25 landscape (S53)

The portrait/canvas PDF drifted on the dialog path (page length 50 != 25mm
pitch). Reverted the PDF fallback to 50x25mm landscape — the size that
seats one-per-label — and rely on the GT800 driver's own Orientation
setting to bring it upright. This gives a zero-install path while Browser
Print (native ZPL, primary button) is set up. QR drawn from the
pre-decoded <img> via addImage.
