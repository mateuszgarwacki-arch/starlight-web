S38b review nav fix + chip strip + timesheets in sidebar

Problem: sidebar "Review" amber badge pointed to /review (Cost Visibility)
which has no inbox — the 2 pending tasks lived at /review/inbox. Badge
felt broken. Also, the new /review/timesheets page from S38 was findable
only by URL.

Fixes:
- Sidebar Review item: when inboxCount > 0, clicking the item now lands
  on /review/inbox (via badgeHref). When count is 0, stays on /review
  (Cost Visibility landing).
- New sidebar item "Timesheets" (Clock icon, red badge) linking directly
  to /review/timesheets with live count of open tbl_timesheet_flags.
- Shared ReviewNavChips component (src/components/review-nav-chips.tsx):
  three-chip nav (Cost Visibility / Workshop Inbox / Timesheet gaps) with
  active state and live counts via realtime channels. Mounted on all
  three pages so you can jump between them regardless of entry point.
- Dropped redundant back-arrow links from inbox and timesheets pages
  (nav chips replace them).

Three uncommitted changes from an earlier incomplete session (the
review-nav-chips component + sidebar tweaks) picked up and completed.
