S41 wrap — move docs to repo + session log

Migrating documentation from project-knowledge uploads to /docs/ in
the repo. Session-to-session updates now ship in the same commit as
the code/migration changes they describe; project knowledge holds
only a 20-line pointer.

Files (all under docs/):

- 00_README.md          new — index, read order, working principles
- 01_overview.md        carried — adds Job Complete to Golden Path
- 02_architecture.md    carried — auth model updated for 6-digit PIN
                        reality, route map updated for /reports/job-close
                        and Complete filtering, API auth section
                        reflects app_metadata-only role read
- 03_database_schema.md rebuilt from live state queries
                        (55 tables, 34 views, 14 RPCs, 2 cron jobs).
                        Old "session N + session N+1 patches" pattern
                        collapsed to a single current-state document.
                        Recent additions section covers S40 → S41
- 04_security_policy.md v2.1 — SP-002 rewritten to match operational
                        reality (6-digit numeric PIN with documented
                        tradeoffs), SP-006/SP-008 strengthened on
                        app_metadata-only reads, SP-014 checklist
                        item 9 added (denormalisation requires
                        trigger + watcher or it must not exist —
                        the S41 tbl_quotes.quote_value drop is
                        cited as the canonical case)
- 05_conventions.md     adds §3.6 (quote-total invariant from S41),
                        §5.1 (single source of truth — promoted from
                        the Quote drift case), §3.8 manual-time-entry
                        note, §3.9 Complete in job statuses, §19
                        (full Job lifecycle conventions)
- TRACKER.md            full S41 session log (S41a–e) + updated
                        cleanup backlog. Notes that S1–S40 history
                        lives in git/old project knowledge

No code changes. Docs only — no deploy needed.